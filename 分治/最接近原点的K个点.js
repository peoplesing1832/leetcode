```js
/**
 * @param {number[][]} points
 * @param {number} K
 * @return {number[][]}
 */
var kClosest = function(points, K) {
    
    // 欧几里得距离
    const getEuclideanDistance = (o1, o2) => {
        const [x1, y1] = o1;
        const [x2, y2] = o2;
        return Math.sqrt((x1 - x2) ** 2 + (y1 - y2) ** 2);
    }
    
    const divideAndConquer = (arr) => {
    }
    
    return reuslt;
};
```
